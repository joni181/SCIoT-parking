# Raspberry Pi node: hardware I/O (sensors, actuators, dispatching).
-r common.txt
RPi.GPIO
spidev
pyserial          # USB serial transport for the Arduino Mega hardware layer
# RFID / Grove drivers as needed - see experiments/ (mfrc522, grovepi).
