# Laptop node: problem generation, AI planner, storage, visualization.
-r common.txt
amqtt            # pure-Python MQTT broker (the laptop hosts the broker; see deploy/broker.py)
dash==4.4.0       # local browser-based operator dashboard (Flask backend)
# AI planner dependency as needed (PDDL/forward-search library or your own).
